<Window x:Class="SimpleGameOverlay.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OverlayWindow"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Topmost="True"
        WindowStartupLocation="Manual">

    <Grid>
        <StackPanel Orientation="Horizontal"
                    Background="#AA000000"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top">
            <StackPanel.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="FontFamily" Value="Segoe UI"/>
                    <Setter Property="FontSize" Value="16"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Margin" Value="10,8,4,8"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                </Style>
                <Style x:Key="ValueStyle" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Margin" Value="0,8,10,8"/>
                </Style>
            </StackPanel.Resources>

            <TextBlock Text="GPU"/>
            <TextBlock x:Name="GpuLoadValue" Text="N/A" Style="{StaticResource ValueStyle}"/>

            <TextBlock Text="GPU°C"/>
            <TextBlock x:Name="GpuTempValue" Text="N/A" Style="{StaticResource ValueStyle}"/>

            <TextBlock Text="CPU"/>
            <TextBlock x:Name="CpuLoadValue" Text="N/A" Style="{StaticResource ValueStyle}"/>

            <TextBlock Text="CPU°C"/>
            <TextBlock x:Name="CpuTempValue" Text="N/A" Style="{StaticResource ValueStyle}"/>
        </StackPanel>
    </Grid>
</Window>